# @package _global_

defaults:
  - _self_
  - local: default

seed: 42
output_dir: ${local.data_dir}/objaverse
input_models_path: ${output_dir}/input_models_path.json 
num_objects: 12031

train_split_size: 0.8
val_split_size: 0.1
test_split_size: 0.1

use_small_dataset: False