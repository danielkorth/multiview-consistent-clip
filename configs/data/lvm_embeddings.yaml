_target_: src.data.datamodule.lvm_embeddings
data_dir: ${local.data_dir}
batch_size: 128 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 1
pin_memory: False
